class Solution {
    public int maxArea(int[] height) {
        int max=Integer.MIN_VALUE;
        int left=0;
        int right=height.length-1;
        if(left<right){
            int lenght=(right-left)*Math.min(right,left);
            max=Math.max(max,lenght);
            
        }
        
    }
}
